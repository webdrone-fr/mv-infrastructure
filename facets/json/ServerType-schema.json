{
    "storages": ["SQL"],
    "default": "The type of the server (volume, ram, ...)",
    "$schema": "http://json-schema.org/draft-07/schema",
    "id": "ServerType",
    "title": "Server Type",
    "type": "object",
    "properties": {"provider": {
        "storages": ["SQL"],
        "nullable": true,
        "description": "Provider",
        "readOnly": false,
        "versionable": false,
        "id": "CE_ServerType_provider",
        "title": "ServerType.provider",
        "$ref": "#/definitions/ServiceProvider"
    }}
}